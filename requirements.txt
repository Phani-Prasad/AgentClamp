# Core dependencies
openai>=1.12.0
python-dotenv>=1.0.0
rich>=13.7.0

# Email
# For Gmail/SMTP, use built-in smtplib (no extra dependency needed)
# For SendGrid (optional):
# sendgrid>=6.11.0

# Database
# SQLite is built-in, no extra dependency needed

# Utilities
pydantic>=2.6.0
