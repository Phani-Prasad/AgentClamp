# ── Web Framework ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# ── Data / Validation ──────────────────────────────────────────
pydantic>=2.6.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0          # SQLite async (local dev)
alembic>=1.13.0            # DB migrations (for cloud PostgreSQL)
asyncpg>=0.29.0            # PostgreSQL async (cloud)

# ── LangChain + LangGraph ──────────────────────────────────────
langchain>=0.2.0
langgraph>=0.1.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# ── LLM Providers ──────────────────────────────────────────────
# Note: We use langchain_openai.ChatOpenAI with custom base_url for all providers
# (OpenAI-compatible endpoints) — no litellm package needed.
groq>=0.9.0                # Groq SDK (free, fast)
anthropic>=0.28.0          # Claude (optional, BYOK)

# ── RAG ────────────────────────────────────────────────────────
chromadb>=0.5.0            # Vector store (local)
sentence-transformers>=3.0.0  # Embeddings (local, no API key)
pypdf>=4.0.0               # PDF loading
# unstructured>=0.14.0    # Multi-format (add later — heavy dependency)

# ── Observability ──────────────────────────────────────────────
# langfuse>=2.0.0          # Add later when setting up observability

# ── Tools ──────────────────────────────────────────────────────
duckduckgo-search>=6.0.0   # Free web search tool
httpx>=0.27.0              # HTTP requests tool

# ── Guardrails + Safety ────────────────────────────────────────
guardrails-ai>=0.4.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
typer>=0.9.0

# ── Utilities ──────────────────────────────────────────────────
websockets>=12.0
rich>=13.7.0
