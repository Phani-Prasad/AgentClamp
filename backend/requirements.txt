# ── Web Framework ──────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# ── Auth ───────────────────────────────────────────────────────
passlib[bcrypt]>=1.7.4        # Password hashing
PyJWT>=2.8.0                  # JWT tokens
email-validator>=2.1.0        # EmailStr validation for Pydantic

# ── Data / Validation ──────────────────────────────────────────
pydantic>=2.6.0
sqlalchemy>=2.0.0
aiosqlite>=0.19.0             # SQLite async (local dev)
alembic>=1.13.0               # DB migrations
asyncpg>=0.29.0               # PostgreSQL async (cloud)

# ── LangChain + LangGraph ──────────────────────────────────────
langchain>=0.2.0
langgraph>=0.1.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0
langchain-text-splitters>=0.2.0

# ── LLM Providers ──────────────────────────────────────────────
groq>=0.9.0                   # Groq SDK (free, fast)
anthropic>=0.28.0             # Claude (optional, BYOK)

# ── RAG ────────────────────────────────────────────────────────
chromadb>=0.5.0               # Vector store (local)
sentence-transformers>=3.0.0  # Embeddings (local, no API key)
pypdf>=4.0.0                  # PDF loading

# ── Tools ──────────────────────────────────────────────────────
duckduckgo-search>=5.0.0,<6.0.0   # Pinned: >=6.0.0 requires Rust build
httpx>=0.27.0                 # HTTP client

# ── Guardrails + Safety ────────────────────────────────────────
guardrails-ai>=0.4.0
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0
typer>=0.9.0

# ── Utilities ──────────────────────────────────────────────────
websockets>=12.0
rich>=13.7.0
